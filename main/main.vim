source $VIMRUNTIME/defaults.vim

" i never want vim messing with me pasting stuff, ever
set paste

" tabs should always be spaces, mostly 4
set tabstop=4
"set softtabstop=0 noexpandtab
set shiftwidth=4
set expandtab
" use retab to convert tabs to spaces
" when working with the file
